# ============================================================================
# DEPRECATED — Use "Deploy to Production" workflow instead
# ============================================================================
#
# This workflow has been replaced by the unified deploy.yml workflow which:
#   - Combines platform + orchestrator deployment
#   - Has dropdown target selection (platform, orchestrator, both)
#   - Has environment approval gates
#   - Has deployment confirmation safety
#   - Has rollback information
#
# To deploy platform:
#   Actions → "Deploy to Production" → Run workflow → target: "platform"
#
# This file is kept for reference but will redirect to the new workflow.
# ============================================================================

name: "[DEPRECATED] Deploy Platform to AWS"

on:
  workflow_dispatch:

jobs:
  redirect:
    name: Redirected to Unified Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Redirect notice
        run: |
          echo "::warning::This workflow is deprecated."
          echo ""
          echo "Please use the unified 'Deploy to Production' workflow instead:"
          echo "  Actions → 'Deploy to Production' → Run workflow → target: 'platform'"
          echo ""
          echo "The unified workflow provides:"
          echo "  - Dropdown target selection (platform, orchestrator, both)"
          echo "  - Environment approval gates"
          echo "  - Deployment confirmation safety"
          echo "  - Pre-flight validation"
          echo "  - Rollback information"
          echo ""
          echo "See: .github/workflows/deploy.yml"
          exit 1
