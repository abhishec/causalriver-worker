<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- NexusBrain Autonomous Trainer -->
    <!-- Runs daily at 1 AM — full 5-stage training pipeline -->
    <key>Label</key>
    <string>com.nexusbrain.trainer</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>/Users/abhishek/bin/nexusbrain-runner.sh</string>
        <string>trainer</string>
    </array>

    <!-- Run at 1:00 AM every day (before consolidation at 2 AM) -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>1</integer>
        <key>Minute</key>
        <integer>0</integer>
    </dict>

    <!-- Logging handled by nexusbrain-runner.sh (appends to ~/Library/Logs/) -->
    <!-- No StandardOutPath/StandardErrorPath — launchd truncates on each run -->

    <!-- Don't restart on crash — wait for next scheduled run -->
    <key>KeepAlive</key>
    <false/>

    <!-- Environment -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/opt/homebrew/sbin:/usr/local/bin:/usr/bin:/bin</string>
        <key>HOME</key>
        <string>/Users/abhishek</string>
    </dict>
</dict>
</plist>
