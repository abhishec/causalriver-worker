{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["app/**", "components/**", "lib/**", "public/**", "scripts/**", "*.ts", "*.js", "*.mjs", "tsconfig.json", "package.json"],
      "outputs": [".next/**", "!.next/cache/**"]
    }
  }
}
