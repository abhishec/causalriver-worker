version: 1
applications:
  - appRoot: platform
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npx next build
      artifacts:
        baseDirectory: .
        files:
          - .next/**/*
          - node_modules/**/*
          - package.json
      cache:
        paths:
          - .next/cache/**/*
          - node_modules/**/*
